services:
  - type: web
    name: dzrt-bot
    env: python
    buildCommand: bash start.sh
    startCommand: python product_monitor_bot.py
    plan: free
    region: oregon
    branch: main
    autoDeploy: true
